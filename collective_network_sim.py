%%MAGIT_PARSER_PROTECT%%```pythonimport numpy as npimport scipy.linalg as linalgimport matplotlib.pyplot as pltimport matplotlib.animation as animationclass CollectiveIntelligenceNetwork:"""A network science pipeline implementing the Kuramoto model and Spectral Graph Theoryto quantify the phase-locking threshold and emergence of collective intelligence."""def init(self, n_nodes: int = 40, connectivity_prob: float = 0.15):self.n_nodes = n_nodesself.p = connectivity_prob# 1. Initialize Network Topology (Erdos-Renyi Random Graph Layout)adj_matrix = np.random.rand(n_nodes, n_nodes) < self.pself.A = np.triu(adj_matrix, k=1).astype(float)self.A = self.A + self.A.Tnp.fill_diagonal(self.A, 0.0)# 2. Compute the Static Degree Matrix (D)degrees = np.sum(self.A, axis=1)self.D = np.diag(degrees)# 3. Compute the Static Graph Laplacian Matrix (L = D - A)self.L = self.D - self.A# 4. Initialize Node Dynamic Variables (Phases and Natural Frequencies)self.phases = np.random.uniform(-np.pi, np.pi, n_nodes)self.natural_freqs = np.random.normal(0.0, 0.5, n_nodes)def get_algebraic_connectivity(self) -> float:"""Extracts the second smallest eigenvalue (Fiedler Value) of the Graph Laplacian.Quantifies how structurally robust the information highway of the network is."""eigenvalues = linalg.eigvalsh(self.L)return eigenvalues[1] if len(eigenvalues) > 1 else 0.0def calculate_order_parameter(self) -> float:"""Computes the global Kuramoto Order Parameter (R).R = 0.0 means absolute phase chaos; R = 1.0 means total phase coherence."""complex_phases = np.exp(1j * self.phases)r = np.abs(np.sum(complex_phases)) / self.n_nodesreturn rdef update_network_dynamics(self, coupling_strength: float, dt: float = 0.05):"""Executes one run-time integration step using the Kuramoto coupling engine.Models how information flow coaxes distinct nodes into geometric harmony."""d_phases = np.zeros(self.n_nodes)for i in range(self.n_nodes):coupled_pull = 0.0for j in range(self.n_nodes):if self.A[i, j] == 1.0:coupled_pull += np.sin(self.phases[j] - self.phases[i])d_phases[i] = self.natural_freqs[i] + (coupling_strength / self.n_nodes) * coupled_pullself.phases = (self.phases + d_phases * dt) % (2 * np.pi)def run_synchronization_analysis(self, total_frames: int = 150):"""Renders the phase visualization. Tracks the exact transition pointwhere the system transforms from separate nodes into a unified macro-mind."""fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 6))r_history = []coupling_schedule = np.linspace(0.1, 15.0, total_frames)fiedler_val = self.get_algebraic_connectivity()def animate(frame):k = coupling_schedule[frame]self.update_network_dynamics(coupling_strength=k)r = self.calculate_order_parameter()r_history.append(r)ax1.clear()ax2.clear()# Plot 1: Polar Phase Circle (Visualizing Phase Alignment)ax1.set_xlim(-1.2, 1.2)ax1.set_ylim(-1.2, 1.2)ax1.set_aspect('equal')ax1.set_title(f"Collective Phase State (K={k:.2f})\nGraph Algebraic Connectivity ($\lambda_2$): {fiedler_val:.3f}")circle = plt.Circle((0, 0), 1.0, edgecolor='gray', fill=False, linestyle='--', alpha=0.5)ax1.add_patch(circle)x = np.cos(self.phases)y = np.sin(self.phases)ax1.scatter(x, y, c=self.phases, cmap='hsv', edgecolors='k', s=80, zorder=3)mean_phase = np.angle(np.sum(np.exp(1j * self.phases)))ax1.arrow(0, 0, r * np.cos(mean_phase), r * np.sin(mean_phase),head_width=0.08, head_length=0.08, fc='black', ec='black', linewidth=2)# Plot 2: Coherence Order Parameter Optimization Curveax2.plot(coupling_schedule[:len(r_history)], r_history, color='darkviolet', linewidth=2)ax2.set_xlim(0, 15)ax2.set_ylim(0, 1.05)ax2.set_title(f"Global Coherence Profile\nKuramoto Order Parameter (R): {r:.3f}")ax2.set_xlabel("Information Coupling Strength (K)")ax2.set_ylabel("Global Coherence (R)")ax2.grid(True, linestyle='--', alpha=0.5)return []ani = animation.FuncAnimation(fig, animate, frames=total_frames, interval=40, blit=False, repeat=False)plt.tight_layout()plt.show()if name == "main":net = CollectiveIntelligenceNetwork(n_nodes=50, connectivity_prob=0.2)net.run_synchronization_analysis(total_frames=120)%%MAGIT_PARSER_PROTECT%%```
